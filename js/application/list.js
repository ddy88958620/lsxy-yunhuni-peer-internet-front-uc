$(document).ready(function() {
    //或
    $('.tabtarget').click(function(){
        var id = $(this).attr('data-toggle');
        console.log(id);
    });

    //初始化modal 数据
    function cleanmodal(){
            
    }
    
    
});
