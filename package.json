{
  "name": "Browserasyc",
  "version": "0.0.1",
  "devDependencies": {
    "bower": "^1.7.9",
    "browser-sync": "^2.13.0",
    "compass": "^0.1.1",
    "gulp": "^3.9.1",
    "gulp-cssmin": "^0.1.7",
    "gulp-jshint": "^2.0.1",
    "gulp-uglify": "^1.5.3"
  },
  "scripts": {
    "sync": "browser-sync start --server './' --files './'",
    "bower": "bower install",
    "syncs": "browser-sync start --server  --files **/*.html",
    "build": "compass compile"
  }
}
